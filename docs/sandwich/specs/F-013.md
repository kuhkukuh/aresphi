# F-013: Catalog Page

> **Module:** Property · **Priority:** 13/100 · **Depends on:** F-006, F-010 · **Source:** prd.md:159-169

Property catalog/listing page with grid layout. Filter by type (jual/sewa), price range, and location. URL-based filter state for shareable links.

## Scope

**In:**
- Grid layout of property cards
- Filter by property type: Jual or Sewa
- Filter by price range
- Filter by location
- URL-based filter state for shareable links

**Out:**
- Map view of properties
- Saved searches
- Pagination (infinite scroll acceptable)

## Acceptance Criteria

- [ ] **AC1** — Property cards display in responsive grid layout
- [ ] **AC2** — Filter by type (Jual/Sewa) updates grid results
- [ ] **AC3** — Filter by price range (min/max) updates grid results
- [ ] **AC4** — Filter by location updates grid results
- [ ] **AC5** — Filter state is reflected in URL for shareable links
- [ ] **AC6** — Clicking a property card navigates to property detail page
- [ ] **AC7** — Page uses ISR with on-demand revalidation when admin updates properties

---

## Mulai kerja

1. Jalankan `superpowers:brainstorming` dengan file ini sebagai starting point —
   scope & AC di atas adalah requirement; task breakdown dan pilihan teknis
   diputuskan di sesi brainstorming (dengan akses codebase).
2. Setelah implementasi, centang tiap AC yang sudah terbukti jalan
   (update `done` di `F-013.json`, lalu jalankan ulang render-specs).

_Generated by /prep — edit `F-013.json`, bukan file ini (akan di-overwrite)._
